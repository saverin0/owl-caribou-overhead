"""Overhead caribou detection: evaluation pipeline.

Our own code. Upstream model definitions live in ``third_party/`` and are
imported only so the public checkpoints can be instantiated.
"""

__version__ = "0.1.0"
